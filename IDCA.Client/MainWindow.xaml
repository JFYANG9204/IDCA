<Window x:Class="IDCA.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:mvvm="clr-namespace:IDCA.Mvvm" 
        d:DataContext="{d:DesignInstance Type=mvvm:NotificationObject}"
        mc:Ignorable="d"
        Title="IBM DataCollection Automation" Height="450" Width="800"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResizeWithGrip"
        Closing="Window_Closing"
        Style="{StaticResource ResourceKey=BaseStyle}">
    <Grid>
        <!-- 阴影效果 -->
        <Grid.Effect>
            <DropShadowEffect ShadowDepth="0" BlurRadius="10" Opacity="0.5"/>
        </Grid.Effect>
        <!-- 标题栏 -->
        <StackPanel Height="40" Style="{StaticResource ResourceKey=BaseStyle}" MouseDown="Window_MouseDown" VerticalAlignment="Top">
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                <!-- 最小化按钮 -->
                <Button Width="50" Height="40" VerticalAlignment="Center" Style="{StaticResource HeaderButtonStyle}" Click="MinimizeButton_OnClick">
                    <Button.Template>
                        <ControlTemplate>
                            <Grid Background="Transparent">
                                <Rectangle Width="12" Height="1.2" Fill="Black"/>
                            </Grid>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <!-- 最大化按钮 -->
                <Button Width="50" Height="40" VerticalAlignment="Center" Style="{StaticResource HeaderButtonStyle}" Click="MaximizeButton_OnClick">
                    <Button.Template>
                        <ControlTemplate>
                            <Grid Background="Transparent">
                                <Rectangle Width="12" Height="12" Stroke="Black" StrokeThickness="1.2"/>
                            </Grid>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <!-- 关闭按钮 -->
                <Button Width="50" Height="40" VerticalAlignment="Center" Style="{StaticResource HeaderButtonStyle}" Click="CloseButton_OnClick">
                    <Button.Template>
                        <ControlTemplate>
                            <Grid Background="Transparent">
                                <Line X1="19" Y1="14" X2="31" Y2="26" Stroke="Black" StrokeThickness="1.2"/>
                                <Line X1="19" Y1="26" X2="31" Y2="14" Stroke="Black" StrokeThickness="1.2"/>
                            </Grid>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
