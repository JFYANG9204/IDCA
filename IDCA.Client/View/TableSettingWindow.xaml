<Window x:Class="IDCA.Client.View.TableSettingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:viewmodel="clr-namespace:IDCA.Client.ViewModel" 
        d:DataContext="{d:DesignInstance Type=viewmodel:TableSettingViewModel}"
        mc:Ignorable="d"
        Height="600" 
        Width="1000"
        Style="{StaticResource IDCA.Window.Normal}">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0"
              Background="{StaticResource IDCA.Colors.Window.Background}">
        </Grid>

        <Grid Grid.Row="2"
              Background="{StaticResource IDCA.Colors.Window.Background}">
        </Grid>

        <Grid Grid.Row="1">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    BorderBrush="{StaticResource IDCA.Colors.Window.BorderBrush}"
                    Background="{StaticResource IDCA.Colors.Window.Background}"
                    BorderThickness="0 0 1 0">

                <TreeView>
                    
                </TreeView>
                
            </Border>
            
        </Grid>
        
        
    </Grid>
</Window>
